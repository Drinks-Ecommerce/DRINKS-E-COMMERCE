/* module.exports = {
    secret: process.env.AUTH_SECRET || "ecommerce",
    expires: process.env.EXPIRES || "24h",
    rounds: process.env.AUTH_ROUNDS || 10,
} */

module.exports = {
    SECRET: 'ecommerce'
}